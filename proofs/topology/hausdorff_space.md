hausdorff_definition(H) ↔
    X topological space
    ∀x,y ∈ X, x ≠ y
    ∃U,V ⊆ X open
    x ∈ U ∧ y ∈ V ∧ U ∩ V = ∅
→

[distinct_limits(L) ↔
    {xₙ} sequence in X
    x,y ∈ X
    xₙ → x ∧ xₙ → y
    → x = y]
→

[closed_points(C) ↔
    A ⊆ X
    {x} closed ∀x ∈ X
    → A closed]
