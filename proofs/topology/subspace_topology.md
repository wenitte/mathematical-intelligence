subspace_definition(S) ↔
    (X,τ) topological space
    Y ⊆ X
    τ_Y = {U ∩ Y : U ∈ τ}
→

[basis_restriction(B) ↔
    𝓑 basis for τ
    𝓑_Y = {B ∩ Y : B ∈ 𝓑}
    → 𝓑_Y basis for τ_Y]
→

[inheritance(I) ↔
    i: Y → X inclusion map
    A ⊆ Y closed in Y
    ↔ ∃C ⊆ X closed
    A = C ∩ Y]
→

[compactness_closed(C) ↔
    Y ⊆ X compact
    X Hausdorff
    → Y closed in X]
