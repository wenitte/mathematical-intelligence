sequential_compactness(S) ↔
    X metric space
    {xₙ} sequence in X
    → ∃{xₙₖ} subsequence
    ∃x ∈ X, xₙₖ → x
→

[total_boundedness(T) ↔
    ∀ε > 0, ∃{x₁,...,xₙ} ⊆ X finite
    X = ∪{B(xᵢ,ε) : 1 ≤ i ≤ n}]
→

[completeness(C) ↔
    {xₙ} Cauchy in X
    → ∃x ∈ X, xₙ → x]
→

[equivalence(E) ↔
    X metric space
    X compact
    ↔ X complete ∧ totally bounded
    ↔ X sequentially compact]
