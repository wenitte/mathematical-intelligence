compact_definition(K) ↔
    X topological space
    {Uᵢ}_{i∈I} open cover of X
    → ∃J ⊆ I finite
    X = ∪{Uᵢ : i ∈ J}
→

[closed_subset(C) ↔
    K ⊆ X compact
    F ⊆ K closed in K
    → F compact]
→

[hausdorff_closed(H) ↔
    X Hausdorff
    K ⊆ X compact
    → K closed in X]
→

[continuous_image(I) ↔
    f: X → Y continuous
    X compact
    → f(X) compact]
