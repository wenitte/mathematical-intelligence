first_countable_definition(F) ↔
    X topological space
    ∀x ∈ X, ∃{Uₙ}_{n∈ℕ} open
    ∀U open, x ∈ U → ∃n ∈ ℕ
    x ∈ Uₙ ⊆ U
→

[second_countable(S) ↔
    ∃𝓑 = {Bₙ}_{n∈ℕ} basis for X]
→

[metrizable_countable(M) ↔
    X metric space
    → X first countable]
→

[sequential_convergence(C) ↔
    X first countable
    A ⊆ X closed
    ↔ ∀{xₙ} ⊆ A convergent
    lim xₙ ∈ A]
